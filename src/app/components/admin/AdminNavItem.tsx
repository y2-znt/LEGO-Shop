import { IconType } from "react-icons/lib";

type AdminNavItemType = {
  selected?: boolean;
  icon?: IconType;
  label?: string;
};

export default function AdminNavItem() {
  return <div>AdminNavItem</div>;
}
